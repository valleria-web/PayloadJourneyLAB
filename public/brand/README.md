# Arquivos da marca

Envie o logo principal para esta pasta com o nome:

`logo.svg`

O caminho publico usado pela aplicacao sera `/brand/logo.svg`.

SVG e o formato recomendado. Se o arquivo estiver disponivel apenas em PNG,
use `logo.png` e preserve o fundo transparente.
