# Document discovery evidence

At execution start, `docs/` contained:

- 91 Markdown files;
- 235 PNG files;
- 3 JSON files.

The principal collections are:

- `docs/ai-readiness/`: four current/recent AI discoverability reports;
- `docs/homepage-refactor/`: 30 Markdown audit/refactor reports plus screenshots;
- `docs/sprints para v3/`: specifications, decisions, closure reports, JSON
  validations and extensive responsive evidence;
- `docs/sprints para v2/` and `docs/sprints/`: historical/superseded sprint
  material;
- `docs/files/`: LAB Definitions and planned LabLog/protocol structures;
- `docs/prompts/`: four design prompts, including HORA.city as founding case.

No paper, white paper file, playlist, music asset, newsletter archive, published
LabLog entry, canonical Case Brief, Checkpoint Plan, Freeze Record, Operational
Payload Path, Detection Report, restoration report or evidence package was
found.

Three white paper entries are placeholders in
`content/payload-journey-lab.ts:1284-1317`; no publication route or document
exists. `components/sections/WhitePapersSection.tsx` renders a LabLog section,
not papers.

Historical documents contain meaningful superseded terminology:

- `docs/files/LAB Definitions.md` uses `Tracing Engineering`;
- V2 and homepage-refactor documents use `Universal System Model Template`;
- historical sources sometimes describe Trace Engineer as a professional.

Current source contains zero `Aura.city` and zero `SMT Core` matches.
