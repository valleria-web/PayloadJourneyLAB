# Link and CTA discovery evidence

## Internal links

Sources scanned:

- `content/site.ts`
- `content/routes.ts`
- `content/methods.ts`
- `content/cases.ts`
- `content/hora-city.ts`
- header, footer, mobile navigation and continuation components

The integrated homepage and navigation verifiers report:

- internal links validated: `true`;
- canonical routes: 11;
- primary navigation items: 5;
- maximum discovery depth: 1;
- maximum route continuations: 4;
- orphan canonical pages: 0;
- unsupported/absent legacy routes guarded: 6.

The current source does not render missing legal routes. `privacy-policy` and
`terms-of-use` remain data-only historical labels.

## External destinations in current branch

1. Udemy course with `FOLLOW-THE-FLOW` coupon.
2. YouTube channel `@PayloadJourneyLAB`.

LinkedIn personal and institutional values, contact and email are `null`.
GitHub and contact calls remain future/non-rendered.

All 11 canonical pages receive the Udemy link through the global footer.
Seven canonical pages contain an explicit HORA.city/case progression path:
`/`, `/learn`, `/cases`, `/protocol`, `/investigation`, `/lab`, `/ecosystem`.

## Risks

- Coupon expiration is `null`.
- There is no repository proof of the Udemy instructor/dashboard state.
- Historical documentation records `@Lab-Log` as a competing YouTube handle.
- No explicit analytics event instrumentation for outbound CTAs was found.
- No public contact/collaboration path is configured.
