# Codex Task — Root README Semantic Reconciliation v1

## Context

The Payload Journey LAB Documentation System Refactor v1 has been completed.

The documentation architecture now explicitly separates:

```text
governance
methods
cases
research
operations
product
archive
```

Current semantic authority is governed by the versioned documentation system.

The repository root `README.md`, however, predates the latest semantic hardening and still contains several formulations that are inconsistent or less precise than the current canonical model.

This task reconciles the **repository root README only** with the current canonical authority.

This is not a documentation-system refactor.

This is not a methodology rewrite.

This is not authorization to change lifecycle or evidence status.

---

# Objective

Update the repository root:

```text
README.md
```

so that an unfamiliar human, developer, researcher or AI agent reading the GitHub repository receives a semantically correct orientation consistent with:

1. Payload Journey LAB Semantic Identity Charter v1.0;
2. AI Welcome v1.0.1;
3. Governance Index;
4. Methods Index;
5. Founding Reference Case 001 — HORA.city active case README.

Preserve the README's useful technical purpose as the repository entry point.

---

# Canonical Sources

Before editing, inspect and treat these as controlling sources:

```text
docs/governance/semantic-identity/semantic-identity-charter-v1.0.md

docs/governance/README.md

docs/methods/README.md

docs/cases/RPJ-HORA-001/README.md
```

Also inspect the current typed/public AI Welcome source where relevant.

Do not resolve a conflict using archived sprint documentation.

Authority order:

```text
governance
    ↓
current versioned method specifications
    ↓
active case records
    ↓
current operational records
    ↓
archive
```

The root README is a **representation** of canonical identity.

It does not override governance.

---

# Scope

Primary file:

```text
README.md
```

Allowed secondary changes only if needed for verification:

* documentation verifier;
* semantic consistency verifier;
* README-specific verification script;
* implementation record.

Do not modify unrelated production content.

Do not rewrite website pages as part of this task.

Do not change public methodology definitions unless the root README exposes a genuine conflict requiring synchronization through an already canonical typed source.

Prefer changing only the README.

---

# Required Semantic Correction 1 — Trace Engineering

The README must explicitly introduce Trace Engineering using the approved ontology.

Use this canonical meaning:

**Trace Engineering is a developing investigative practice composed of capabilities for tracing operations, locating decisions, comparing expected models with execution, and producing verifiable explanations.**

Equivalent wording may be used for readability, but all of these semantic components must remain:

```text
Trace Engineering
→ developing investigative practice
→ composed of capabilities
→ tracing operations
→ locating decisions
→ comparing expected models with execution
→ producing verifiable explanations
```

Do not present Trace Engineering as:

* an established engineering discipline;
* an industry standard;
* an externally standardized field;
* a recognized profession.

---

# Required Semantic Correction 2 — Trace Engineer

The current README contains language equivalent to:

```text
A professional role or investigative function...
```

This must be corrected.

The approved current relationship is:

```text
Trace Engineering
→ developing investigative practice

Trace Engineer
→ developing investigative function/profile applying that practice
```

Use wording equivalent to:

**Trace Engineer is a developing investigative function/profile applying Trace Engineering capabilities. It is not presented by Payload Journey LAB as an established profession or universally recognized occupational role.**

Do not use:

```text
professional role
profession
recognized profession
official job category
certified role
```

as current unqualified descriptions.

Do not imply certification.

---

# Required Semantic Correction 3 — HORA.city Case Identity

The current README describes HORA.city primarily as a "real case study."

Reconcile this with the current canonical identity.

Preferred framing:

# Founding Reference Case 001 — HORA.city

Include:

```text
Investigation ID: RPJ-HORA-001
Lifecycle: Active investigation
Relationship: founder-owned internal reference investigation
```

It is acceptable to explain that the case operates on a real software system.

However, the README must make clear that it is:

* internal;
* founder-owned;
* active;
* not external independent validation.

Avoid language that could cause an unfamiliar reader to infer:

* external client engagement;
* independent case validation;
* scientific validation;
* completed investigation.

---

# Required Semantic Correction 4 — HeartCreated Evidence Status

The current README associates:

```text
createdAt
→ HeartCreated
```

Preserve the association but qualify its evidence status.

The README must communicate that:

**HeartCreated is currently a documentary association within the investigation. Runtime confirmation has not yet been established in the public evidence record.**

Do not state or imply that:

* HeartCreated was observed at runtime;
* HeartCreated caused the anomaly;
* HeartCreated is the origin;
* the first divergence has been located.

Current known status remains bounded.

---

# Required Semantic Correction 5 — Case Outcome Guardrails

The HORA.city section must preserve:

```text
Lifecycle: Active investigation
```

The README must not claim that the investigation has established:

* causal origin;
* temporal authority;
* restoration;
* verified fix;
* final outcome;
* closure.

If a compact status block exists, update it to reflect current state accurately.

For example:

```text
Case: Founding Reference Case 001 — HORA.city
Investigation ID: RPJ-HORA-001
Anomaly: incorrect createdAt
Associated payload: HeartCreated
Association evidence: documentary
Method: Reverse Payload Journey
Mission: Track to Origin
Lifecycle: Active investigation
```

Do not invent additional evidence.

---

# Required Semantic Correction 6 — USMT

Preserve the canonical expansion:

```text
USMT — Universal System Modeling Template
```

If describing its status, use:

```text
Canonical within Payload Journey LAB
```

Do not imply that USMT is:

* an external industry standard;
* universally standardized;
* formally recognized outside the LAB.

The README may remain concise.

It does not need to reproduce the full governance disclaimer unless required for clarity.

---

# Required Semantic Correction 7 — Methods vs Developing Concepts

The README should distinguish current method/concept lifecycle where useful.

Current orientation should not flatten everything into equivalent maturity.

At minimum preserve:

```text
Payload Journey
USMT
Reverse Payload Journey
Operational Payload Path
Track to Origin
```

as current LAB methods/concepts according to canonical sources.

Preserve:

```text
Track Mode
```

as an investigative operating practice.

Preserve:

```text
Trace Engineering
```

as a developing investigative practice.

Preserve:

```text
Trace Engineer
```

as a developing investigative function/profile.

Do not create standalone canonical specifications where none exist.

The Methods Index currently states that the six method domains have no standalone canonical versioned specification published yet.

The root README must not imply otherwise.

---

# Required Semantic Correction 8 — Evidence Model

Add a concise reference to the:

# Payload Journey LAB Evidence Model

Do not reproduce the entire model unless doing so improves the README materially.

A short statement is sufficient, such as:

**The LAB distinguishes six evidence levels—Conceptual, Documentary, Runtime, Implementation, Verification, and External or Independent Evidence—to prevent different kinds of claims from being conflated.**

If useful, link readers to the canonical governance or AI Welcome source.

Do not describe the model as an external scientific standard.

---

# Required Semantic Correction 9 — Documentation System Discovery

The Documentation System Refactor has created a governed documentation entry point.

Add a concise section such as:

```text
## Documentation
```

or equivalent.

Point readers to:

```text
docs/README.md
```

Explain briefly that it distinguishes:

* current governance;
* method indexes/specifications;
* active cases;
* research;
* operations;
* product documentation;
* historical archive.

Also point to:

```text
docs/governance/semantic-identity/semantic-identity-charter-v1.0.md
```

as the current semantic governance source where appropriate.

The root README remains repository orientation.

`docs/README.md` becomes documentation-system orientation.

---

# Preserve Existing Useful Repository Information

Do not remove useful technical information merely because it is not semantic governance.

Preserve and update where accurate:

* project purpose;
* website scope;
* technical stack;
* local development commands;
* repository scope;
* AI/crawler visibility;
* license status;
* author information.

Verify these statements against the current repository before preserving them.

If any technical claim is stale, update only when the repository itself provides clear evidence.

Do not browse external accounts for this task unless needed to verify a claim that cannot be determined locally.

---

# Founder Wording

Preserve attribution to:

**Valéria dos Santos Reiser**

Align wording with the Semantic Identity Charter.

Do not overstate:

* academic position;
* institutional affiliation;
* scientific recognition;
* professional title;
* external validation.

Authorship and intellectual direction should remain explicit.

---

# Slogan

Preserve the canonical slogan:

**Follow the flow. Understand the system.**

If the Portuguese repository subtitle:

**Siga o flow. Entenda o sistema.**

is intentionally part of the repository presentation, it may remain as a localized expression.

Do not replace the canonical English slogan in governance.

The README may use both where helpful.

---

# Archive Safeguard

Do not use archived documents as current semantic authority.

Historical material may contain:

* `Tracing Engineering`;
* old USMT expansions;
* profession wording for Trace Engineer;
* earlier HORA.city framing.

Those historical forms must not be reintroduced into the root README.

---

# Semantic Verification

After editing, verify the root README against the current canonical sources.

At minimum assert:

1. `Trace Engineering` is present.
2. Trace Engineering is described as a developing investigative practice.
3. It is described as composed of capabilities.
4. Trace Engineer is not described as an established profession.
5. The phrase `professional role` is removed or contextually guarded.
6. HORA.city is identified as Founding Reference Case 001.
7. `RPJ-HORA-001` is preserved.
8. HORA.city lifecycle is Active Investigation.
9. HORA.city is identified as internal/founder-owned or equivalent.
10. No external-validation claim exists.
11. `HeartCreated` is identified as documentary association.
12. No runtime confirmation of HeartCreated is claimed.
13. No causal origin is claimed.
14. No restoration is claimed.
15. No verification is claimed.
16. `Universal System Modeling Template` remains correct.
17. USMT is not presented as an external standard.
18. Payload Journey LAB Evidence Model is referenced.
19. `docs/README.md` is discoverable from the root README.
20. the Semantic Identity Charter is discoverable where appropriate.
21. no `Tracing Engineering` appears as current terminology.
22. no `Aura.city` appears.
23. no current canonical claim is derived from archive.

---

# Link Verification

Verify every new Markdown link added to the root README.

At minimum check:

```text
docs/README.md
docs/governance/semantic-identity/semantic-identity-charter-v1.0.md
docs/cases/RPJ-HORA-001/README.md
```

if included.

No broken links.

---

# Documentation Record

Create a concise operational implementation record under the current documentation system.

Suggested destination:

```text
docs/operations/documentation/
2026-07-31-root-readme-semantic-reconciliation-v1.md
```

Record:

* reason for reconciliation;
* canonical sources used;
* semantic conflicts found;
* corrections applied;
* unchanged technical sections;
* verification results;
* unresolved issues if any.

This record is operational evidence.

It is not governance authority.

---

# Git Discipline

Before editing:

```text
git status --short
```

After editing:

* inspect diff;
* distinguish semantic edits from unrelated existing changes;
* do not discard user changes;
* do not modify archived historical material;
* do not commit;
* do not push.

Expected primary modification:

```text
README.md
```

plus implementation record and verification changes only if needed.

---

# Final Report Required

Return:

## 1. Root README reconciliation summary

Explain what was corrected.

## 2. Canonical sources used

List exact files.

## 3. Trace Engineering

Quote/report the final semantic relationship now represented in the README.

## 4. Trace Engineer

Confirm it is no longer presented as an established professional role.

## 5. HORA.city

Confirm:

```text
Founding Reference Case 001 — HORA.city
RPJ-HORA-001
Active investigation
internal/founder-owned
```

## 6. HeartCreated

Confirm its evidence level/status remains documentary association and not runtime-confirmed.

## 7. Evidence Model

Confirm the Payload Journey LAB Evidence Model is discoverable.

## 8. Documentation discovery

Confirm root README links into the governed `docs/` system.

## 9. Verification

Report:

* semantic verifier;
* Markdown links;
* TypeScript if run;
* build if run;
* existing relevant verification suites.

## 10. Files changed

List all modified/created files.

## 11. Git status

Confirm:

* no commit;
* no push;
* no unrelated changes introduced.

---

# Definition of Done

The task is complete when an unfamiliar reader can open the repository root README and correctly understand:

```text
what Payload Journey LAB is
who created it
what its current methods are
what Trace Engineering currently is
what Trace Engineer currently is not
what HORA.city represents
what RPJ-HORA-001 means
that the case remains active
that HeartCreated is documentary, not runtime-confirmed
that evidence levels are distinguished
where canonical documentation lives
```

The root README must become a semantically faithful repository representation of the governed Payload Journey LAB identity.

It must not become a second governance authority.

## Execute without commit or push.
